---
description: 禁止在生成代码后自动创建 Markdown 解释文档
alwaysApply: true
---

# 禁止自动生成 Markdown 文档

## 核心规则

**严格禁止**在生成或修改代码后自动创建任何 Markdown 解释文档（.md 文件）。

## 具体要求

1. **不要创建文档文件**

   - 不要创建 `CHANGELOG.md`、`FIX.md`、`SOLUTION.md`、`GUIDE.md` 等任何 Markdown 文档
   - 不要创建任何以 `.md` 结尾的解释性文件
   - 不要创建临时文档或说明文件

2. **代码即文档**

   - 代码应该通过清晰的命名、注释和结构自解释
   - 只在代码中添加必要的注释，不要创建额外的文档文件

3. **响应方式**

   - 如果需要解释代码，直接在聊天中说明
   - 不要通过创建文件的方式来解释代码
   - 代码修改完成后，直接说明修改内容，不要生成文档

4. **例外情况**
   - 用户**明确要求**创建文档时，可以创建
   - 项目已有的文档（如 README.md）可以更新，但不要创建新的解释性文档

## 违反示例

❌ **错误行为：**

- 生成代码后创建 `FIX-SUMMARY.md`
- 创建 `WINDOWS-TTS-SOLUTION.md`
- 创建 `CHANGELOG.md` 或 `CHANGES.md`
- 创建任何临时说明文档

✅ **正确行为：**

- 生成代码后直接在聊天中说明修改内容
- 如果需要详细说明，在聊天中提供，不要创建文件
- 只修改用户明确要求修改的现有文档

## 执行原则

当完成代码修改后：

1. ✅ 直接说明修改了什么
2. ✅ 在聊天中提供简要说明
3. ❌ 不要创建任何 `.md` 文件
4. ❌ 不要创建临时文档

记住：**代码修改完成后，说明在聊天中，不要创建文档文件。**
