# Sherpa-ONNX TTS 故障排查指南

当 Sherpa-ONNX TTS 在 Windows 平台遇到问题时，本指南将帮助您诊断和解决问题。

## 📍 日志文件位置

错误日志会自动保存到以下位置：

### Windows

```
C:\Users\<你的用户名>\AppData\Roaming\RWKV-VTuber\logs\
```

### macOS

```
~/Library/Application Support/RWKV-VTuber/logs/
```

### Linux

```
~/.config/RWKV-VTuber/logs/
```

日志文件名格式：`sherpa-tts-error-<时间戳>.log`

## 🔍 日志文件包含的信息

每个日志文件会记录：

1. **系统信息**

   - 操作系统平台和架构
   - CPU 信息和核心数
   - 总内存和可用内存
   - Node.js 版本

2. **资源检查**

   - 实时内存使用情况
   - 进程内存使用
   - 资源充足性警告

3. **模型文件验证**

   - 模型文件路径
   - 文件大小
   - 路径格式警告（中文、空格等）

4. **操作详情**

   - TTS 实例创建过程
   - 语音生成参数
   - 每步操作的耗时
   - 详细的错误堆栈

5. **错误诊断**
   - 错误码分析
   - 可能的原因
   - 建议的解决方案

## 🐛 常见问题和解决方案

### 问题 1: 返回错误码（如 17444296, 14293000）

**症状**: 控制台显示类似 `Error invoking remote method 'sherpa_tts_generate': 17444296` 的错误

**可能原因**:

1. **路径格式问题** - 模型文件路径包含中文、空格或特殊字符
2. **模型文件损坏** - 文件不完整或下载失败
3. **内存不足** - 系统可用内存太低
4. **权限问题** - 无法访问模型文件

**解决方案**:

#### 方案 A: 修改模型路径（推荐）

1. 打开应用配置页面
2. 找到 Sherpa TTS 配置
3. 将模型文件移动到纯英文路径，例如：
   ```
   C:\Models\sherpa-tts\matcha-icefall-zh-baker\
   ```
   ❌ 避免这样的路径：
   ```
   C:\用户\张三\Documents\模型文件\
   C:\Program Files\My TTS Models\
   ```

#### 方案 B: 检查系统资源

1. 打开任务管理器（Ctrl+Shift+Esc）
2. 查看内存使用情况
3. 关闭不必要的应用程序
4. 确保至少有 1GB 可用内存

#### 方案 C: 重新下载模型

1. 删除现有模型文件
2. 在应用中重新下载
3. 确保下载完成且文件完整

#### 方案 D: 使用其他 TTS 服务

如果 Sherpa TTS 持续失败，可以切换到其他服务：

- **MiniMax TTS** - 在线 API 服务，无需本地模型
- **Web Speech API** - 浏览器内置语音合成

### 问题 2: 内存不足

**症状**: 日志中显示 "可用内存不足" 或 "系统内存使用率过高"

**解决方案**:

1. 关闭其他应用程序释放内存
2. 重启计算机
3. 如果是 32 位系统，建议升级到 64 位
4. 考虑增加物理内存

### 问题 3: Windows 32 位系统限制

**症状**: 日志中显示 "检测到 32 位 Windows 系统"

**解决方案**:

- 32 位系统内存限制为 4GB，可能无法运行大型模型
- 建议升级到 64 位 Windows 系统
- 或使用在线 TTS 服务（如 MiniMax）

### 问题 4: 模型文件路径包含空格

**症状**: 日志中显示 "路径包含空格" 警告

**解决方案**:
将模型移动到不含空格的路径：

```
✅ 正确: C:\Models\sherpa-tts\
❌ 错误: C:\Program Files\My Models\
```

## 📋 如何查看日志文件

### Windows

1. 按 `Win + R` 打开运行对话框
2. 输入 `%APPDATA%\RWKV-VTuber\logs`
3. 按回车打开日志文件夹
4. 用记事本或 VS Code 打开最新的日志文件

### macOS

1. 打开 Finder
2. 按 `Cmd + Shift + G`
3. 输入 `~/Library/Application Support/RWKV-VTuber/logs`
4. 按回车打开日志文件夹

### Linux

1. 打开终端
2. 执行: `cd ~/.config/RWKV-VTuber/logs`
3. 执行: `ls -lt` 查看最新日志
4. 执行: `cat <文件名>` 查看内容

## 🔧 高级排查步骤

### 1. 验证模型文件完整性

```bash
# Windows PowerShell
Get-ChildItem "C:\你的模型路径" -Recurse | Select-Object Name, Length

# macOS/Linux
find /你的模型路径 -type f -exec ls -lh {} \;
```

检查关键文件是否存在：

- `model-steps-3.onnx` (约 100-200 MB)
- `vocos-22khz-univ.onnx` (约 20-30 MB)
- `lexicon.txt`
- `tokens.txt`

### 2. 检查文件权限

```bash
# Windows: 右键点击文件夹 -> 属性 -> 安全
# 确保当前用户有读取权限

# macOS/Linux
ls -la /你的模型路径
chmod -R 755 /你的模型路径
```

### 3. 监控系统资源

打开日志文件，查找以下关键信息：

```
系统资源检查
平台: win32
总内存: 16.00 GB
可用内存: 2.50 GB  <-- 应该大于 0.5 GB
内存使用率: 84.38%  <-- 应该小于 90%
```

### 4. 分析错误码

如果看到错误码，对照以下信息：

| 错误码类型  | 可能原因                 |
| ----------- | ------------------------ |
| 1000-9999   | 配置错误                 |
| 10000-99999 | 文件访问错误             |
| 100000+     | 内存地址（路径解析失败） |

大数字（如 17444296）通常表示内存地址，意味着 native 模块返回了指针而不是对象，这通常是由于：

- 路径格式问题
- 文件读取失败
- 内存分配失败

## 📞 获取帮助

如果以上方法都无法解决问题，请：

1. **收集信息**:

   - 完整的日志文件
   - 系统信息（Windows 版本、内存大小）
   - 模型文件路径
   - 错误截图

2. **提交 Issue**:

   - 访问项目 GitHub Issues 页面
   - 创建新的 Issue
   - 附上收集的信息

3. **临时方案**:
   - 切换到 MiniMax TTS 或其他服务
   - 使用在线版本（如果可用）

## ✅ 成功运行的检查清单

在尝试使用 Sherpa TTS 前，请确认：

- [ ] 系统是 64 位 Windows
- [ ] 可用内存至少 1GB
- [ ] 模型文件路径纯英文（无中文、无空格）
- [ ] 所有模型文件下载完整
- [ ] 有足够的磁盘空间（至少 500MB）
- [ ] 杀毒软件未阻止应用访问文件
- [ ] 查看了最新的错误日志文件

## 🌟 最佳实践

1. **模型存放位置**:

   ```
   推荐: C:\Models\rwkv-vttuber\tts-models\
   ```

2. **定期清理日志**:

   - 日志文件会不断累积
   - 定期删除旧日志释放空间

3. **监控性能**:

   - 首次使用时关注内存使用
   - 确认 TTS 合成速度正常

4. **备选方案**:
   - 配置好 MiniMax TTS 作为备用
   - 遇到问题时可快速切换

---

**提示**: 大多数 Windows 平台的问题都与路径格式有关。将模型文件移动到纯英文路径通常能解决 80% 的问题！
