# 关于更新包大小的说明

## 📦 为什么第一次更新这么大？

### 增量更新工作原理

`electron-updater` 使用 **blockmap** 技术实现增量更新：

1. **第一次更新（全量下载）**
   ```
   用户使用版本：1.2.4
   最新版本：1.2.5
   
   问题：用户电脑上没有 1.2.4 的 blockmap 文件
   结果：必须下载完整的 1.2.5 安装包（~170MB）
   ```

2. **第二次更新（增量下载）**
   ```
   用户使用版本：1.2.5（已有 blockmap）
   最新版本：1.2.6
   
   过程：
   1. electron-updater 对比本地 blockmap 和远程 blockmap
   2. 只下载变化的文件块
   
   结果：只下载差异部分（通常只有几MB到几十MB）
   ```

## 🔍 Blockmap 文件的作用

- `.exe.blockmap` 文件记录了安装包中每个块的哈希值
- electron-updater 通过对比 blockmap 确定哪些块需要更新
- 只下载变化的块，拼接成新版本

## 📊 实际更新大小预期

| 更新类型 | 大小 | 原因 |
|---------|------|------|
| **首次更新** | ~170MB | 全量下载（无 blockmap） |
| **小改动更新** | 5-20MB | 只更新少量代码文件 |
| **中等改动** | 20-50MB | 更新多个模块 |
| **大改动** | 50-100MB | 更新大量文件或依赖 |

## ✅ 已应用的优化配置

### 1. 启用差分包
```json
{
  "nsis": {
    "differentialPackage": true
  }
}
```

### 2. 最大压缩
```json
{
  "compression": "maximum"
}
```

### 3. 正确的发布配置
```json
{
  "publish": {
    "provider": "github",
    "releaseType": "release"
  }
}
```

## 🎯 下次更新测试

**从 v1.2.5 更新到 v1.2.6 时：**
- ✅ 应该能看到增量更新生效
- ✅ 下载大小应该大幅减小
- ✅ 只下载变更的部分

## 💡 给用户的说明

可以在更新提示中添加说明：

```
首次更新需要下载完整安装包（约170MB）
后续更新将使用增量更新，只下载变更部分（通常小于50MB）
```

## 🔧 技术细节

### electron-updater 更新流程

```
1. 应用启动，检查更新
   ↓
2. 从 GitHub Release 获取 latest.yml
   ↓
3. 对比版本号
   ↓
4. 如果有新版本：
   a) 检查本地是否有旧版本的 blockmap
   b) 如果有：下载新版本的 blockmap，对比差异
   c) 如果没有：下载完整安装包
   ↓
5. 下载文件（增量或全量）
   ↓
6. 验证文件完整性（SHA512）
   ↓
7. 安装新版本
```

## 📝 结论

- **第一次更新大是正常的**，因为需要完整下载
- **从第二次开始**，增量更新会显著减小下载大小
- 这是 electron-updater 的标准行为，不是配置问题

## 🚀 验证增量更新

要验证增量更新是否生效：

1. 安装 v1.2.5
2. 发布 v1.2.6（只修改少量代码）
3. 检查更新，观察下载大小
4. 如果下载大小<50MB，说明增量更新生效 ✅

---

**注意**：如果每次更新都很大，可能的原因：
1. 修改了大量文件（如依赖更新）
2. blockmap 文件损坏
3. 用户清理了缓存（导致 blockmap 丢失）

